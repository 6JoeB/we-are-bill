export enum Role {
    Standard,
    Storyteller,
    Bill
}

export enum Phase {
    Lobby,
    StorytellerPick,
    GoalPick,
    Playing
}

export enum PlayingPhase {
    ChooseAction,
    VoteOnAction,
    RollOnAction
}